<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.house.deed.pavilion.module.houseTag.mapper.HouseTagMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.house.deed.pavilion.module.houseTag.entity.HouseTag">
        <id column="id" property="id" />
        <result column="tenant_id" property="tenantId" />
        <result column="house_id" property="houseId" />
        <result column="tag_id" property="tagId" />
        <result column="create_time" property="createTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, tenant_id, house_id, tag_id, create_time
    </sql>

    <!-- 查询标签ID列表 -->
    <select id="selectTagIdsByHouseId" resultType="java.lang.Long">
        SELECT tag_id
        FROM house_tag
        WHERE house_id = #{houseId}
        <!-- 插件会自动添加：AND tenant_id = #{tenantId} -->
    </select>

    <!-- 删除关联关系 -->
    <delete id="deleteByHouseIdAndTagIds">
        DELETE FROM house_tag
        WHERE house_id = #{houseId}
        AND tag_id IN
        <foreach collection="tagIds" item="tagId" open="(" separator="," close=")">
            #{tagId}
        </foreach>
        <!-- 插件会自动添加：AND tenant_id = #{tenantId} -->
    </delete>

</mapper>
