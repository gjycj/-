### **一、房源模块（house）权限控制**

#### 核心目标：仅允许经纪人查询 / 操作自己创建的房源（通过`create_agent_id`关联）

| 序号 | 代办任务                   | 涉及文件                           | 操作说明                                                     | 验收标准                                                     |
| ---- | -------------------------- | ---------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| 1.1  | 房源分页查询添加创建人过滤 | `HouseServiceImpl.getHousePage`    | 在分页查询条件中添加 `eq(House::getCreateAgentId, currentAgentId)` | 经纪人调用分页接口时，仅返回`create_agent_id`为当前经纪人 ID 的房源 |
| 1.2  | 房源删除前校验创建人权限   | `HouseServiceImpl.deleteAndBackup` | 新增校验：查询房源的`create_agent_id`，需与当前经纪人 ID 一致 | 非创建人删除房源时，抛出`403 无权操作`异常                   |
| 1.3  | 房源更新操作添加创建人校验 | （假设存在`updateHouse`方法）      | 在更新前校验房源的`create_agent_id`是否为当前经纪人 ID       | 非创建人更新房源时，抛出`403 无权操作`异常                   |
| 1.4  | 房源详情查询过滤创建人     | （假设存在`getHouseById`方法）     | 查询时添加`create_agent_id = currentAgentId`条件             | 经纪人无法查询他人创建的房源详情                             |

### **二、客户模块（customer）权限控制**

#### 核心目标：仅允许经纪人查询 / 跟进自己创建的客户（通过`create_agent_id`关联）

| 序号 | 代办任务                      | 涉及文件                                      | 操作说明                                                     | 验收标准                                           |
| ---- | ----------------------------- | --------------------------------------------- | ------------------------------------------------------------ | -------------------------------------------------- |
| 2.1  | 客户查询接口添加创建人过滤    | （客户 Service 的分页 / 列表方法）            | 在查询条件中添加 `eq(Customer::getCreateAgentId, currentAgentId)` | 经纪人仅能查询`create_agent_id`为自己的客户        |
| 2.2  | 客户跟进记录添加创建人校验    | `CustomerFollowUpController.addFollowUp`      | 新增校验：查询客户的`create_agent_id`，需与当前经纪人 ID 一致 | 非客户创建人添加跟进记录时，抛出`403 无权跟进`异常 |
| 2.3  | 客户更新 / 删除操作校验创建人 | （客户 Controller/Service 的更新 / 删除方法） | 操作前校验客户的`create_agent_id`是否为当前经纪人 ID         | 非创建人操作客户时，抛出`403 无权操作`异常         |

### **三、带看记录模块（visit_record）权限控制**

#### 核心目标：仅允许经纪人操作自己创建的带看记录，且带看的客户 / 房源必须是自己创建的

| 序号 | 代办任务                      | 涉及文件                                                     | 操作说明                                                 | 验收标准                                           |
| ---- | ----------------------------- | ------------------------------------------------------------ | -------------------------------------------------------- | -------------------------------------------------- |
| 3.1  | 带看记录查询过滤经纪人 ID     | `VisitRecordService`的查询方法（如`getByHouseId`、`getByContractId`） | 添加 `eq(VisitRecord::getAgentId, currentAgentId)` 条件  | 经纪人仅能查询自己创建的带看记录                   |
| 3.2  | 创建带看记录时校验客户归属    | `VisitRecordController.createVisitRecord`                    | 新增校验：客户表的`create_agent_id`必须等于当前经纪人 ID | 带看他人创建的客户时，抛出`403 无权带看该客户`异常 |
| 3.3  | 创建带看记录时校验房源归属    | `VisitRecordController.createVisitRecord`                    | 新增校验：房源表的`create_agent_id`必须等于当前经纪人 ID | 带看他人创建的房源时，抛出`403 无权带看该房源`异常 |
| 3.4  | 带看记录更新 / 删除校验创建人 | （带看记录的更新 / 删除方法）                                | 校验带看记录的`agent_id`是否为当前经纪人 ID              | 非创建人操作带看记录时，抛出`403 无权操作`异常     |

### **四、通用权限机制优化**

#### 核心目标：通过统一切面减少重复代码，确保权限校验无遗漏

| 序号 | 代办任务              | 涉及文件                                             | 操作说明                                                     | 验收标准                               |
| ---- | --------------------- | ---------------------------------------------------- | ------------------------------------------------------------ | -------------------------------------- |
| 4.1  | 定义数据权限注解      | 新增`@AgentDataPermission`注解                       | 注解参数指定实体类的创建人字段（如`createAgentId`、`agentId`） | 注解可标记需要权限控制的方法           |
| 4.2  | 实现 AOP 切面拦截权限 | 新增`AgentDataPermissionAspect`                      | 拦截标记注解的方法，自动在查询条件中添加`创建人=当前经纪人ID` | 无需手动编写过滤条件，切面自动生效     |
| 4.3  | 切面适配不同实体类    | 切面中通过反射获取实体的创建人字段，动态拼接查询条件 | 支持房源（`createAgentId`）、客户（`createAgentId`）、带看记录（`agentId`）等 | 不同实体的权限过滤通过注解参数统一配置 |

### **五、其他关联模块补充**

#### 核心目标：确保依赖模块（如维修工单、合同）的权限继承主数据权限

| 序号 | 代办任务                   | 涉及文件                               | 操作说明                                                     | 验收标准                         |
| ---- | -------------------------- | -------------------------------------- | ------------------------------------------------------------ | -------------------------------- |
| 5.1  | 维修工单查询关联房源创建人 | `MaintenanceOrderService.getByHouseId` | 先查询房源的`create_agent_id`，再过滤工单关联的房源属于当前经纪人 | 经纪人仅能查看自己房源的维修工单 |
| 5.2  | 合同查询关联经纪人权限     | `ContractService.getByHouseId`         | 合同关联的房源`create_agent_id`需等于当前经纪人 ID           | 经纪人仅能查看自己房源的合同     |

### **六、测试与验证**

| 序号 | 代办任务                 | 操作说明                                             | 验收标准                                        |
| ---- | ------------------------ | ---------------------------------------------------- | ----------------------------------------------- |
| 6.1  | 单元测试覆盖权限校验逻辑 | 为每个权限校验点编写测试用例（正常访问、越权访问）   | 越权操作时均抛出 403 异常，正常操作返回正确数据 |
| 6.2  | 集成测试验证端到端权限   | 模拟经纪人账号操作他人数据，检查接口返回是否符合预期 | 所有接口严格遵守 “仅操作自己创建的数据” 规则    |

### 实施优先级建议

1. 优先完成 **房源、客户、带看记录的核心 CRUD 权限控制**（1.1、1.2、2.1、2.2、3.1、3.2、3.3），覆盖核心业务场景；
2. 其次实现 **通用 AOP 切面**（4.1、4.2、4.3），减少重复代码；
3. 最后补充 **关联模块权限**（5.1、5.2）和 **测试验证**（6.1、6.2）。

通过以上步骤，可逐步实现 “经纪人仅能操作自己创建的数据” 的权限细化目标，同时保证代码可维护性。